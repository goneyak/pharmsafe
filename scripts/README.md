These scripts were used to transform and normalize medication information into structured records for the MVP.

They are retained for transparency, reproducibility, and future cleanup. They are not part of the runtime application.
